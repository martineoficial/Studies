**Lição 6: JavaScript Assíncrono: `Async`/`Await`**

- **Tema:** Simplificando o código assíncrono para torná-lo mais legível.
- **Objetivo:** Reescrever lógica baseada em `Promises` usando a sintaxe mais síncrona de `async`/`await`.
- **Conteúdo:**
    - Sintaxe `async function` e a palavra-chave `await`.
    - Tratamento de erros com `try...catch` em `async`/`await`.
    - Refatoração de exemplos da Lição 5.
- **Repositório:** `6.async-await`
- **Estrutura:** `index.html`, `src/lesson6.js` (exemplos com `Workspace` real para uma API pública, como `jsonplaceholder.typicode.com`).